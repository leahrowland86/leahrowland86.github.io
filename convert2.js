function tempConvert(temp) {
   let cTemp = Number(document.getElementById("id1").value);
    debugger;
    return "Temperature in Celsius: " + cTemp + " degrees \nTemperature in Fahrenheit: " + (((cTemp * 9) / 5) + 32) + " degrees";
}
